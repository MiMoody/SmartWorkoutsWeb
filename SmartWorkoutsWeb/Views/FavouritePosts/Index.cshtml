@model IEnumerable<SmartWorkoutsWeb.Models.FavouritePosts>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Posts.NamePost)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Users.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Posts.NamePost)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFavPost }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFavPost }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFavPost })
        </td>
    </tr>
}

</table>
